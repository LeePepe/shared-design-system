# CLAUDE.md

Read `AGENTS.md` first and follow it; it is the only source of repository rules.

Claude-specific: use `scripts/verify` rather than ad-hoc xcodebuild invocations; it picks the simulator.
